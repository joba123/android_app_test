<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Erinnerungen zum Testtermin. Ab Android 13 muss der Nutzer sie
         zusaetzlich bestaetigen; die App fragt erst danach, wenn er die
         Erinnerungen einschaltet. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Ohne diese Berechtigung waeren nach einem Neustart des Geraets alle
         geplanten Erinnerungen verloren. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- In-App-Kaeufe ueber Google Play. -->
    <uses-permission android:name="com.android.vending.BILLING" />
    <!-- AdMob und die Einwilligungsabfrage brauchen Netz. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="Einstellungstest Trainer"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- AdMob-App-ID. Hier steht Googles oeffentliche TEST-ID; vor einer
             Veroeffentlichung durch die eigene ID aus der AdMob-Konsole
             ersetzen. Fehlt der Eintrag, stuerzt die App beim Start des
             Ad-SDK ab - deshalb steht er auch im Testbuild. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

        <!-- Stellt geplante Erinnerungen nach einem Neustart wieder her.
             Gehoert zu flutter_local_notifications. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>
